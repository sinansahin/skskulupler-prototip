<%= simple_form_for([:admin, @info]) do |f| %>
  <% if @info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@info.errors.count, "error") %> prohibited this club from being saved:</h2>

      <ul>
      <% @info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :title %>
  </div>
    <div class="field">
    <%= f.input :content %>
     </div>
      <div class="field">
    <%= f.input :image %>
     </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
