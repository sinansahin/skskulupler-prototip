<%= simple_form_for([:admin, @activity]) do |f| %>
  <% if @activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@activity.errors.count, "error") %> prohibited this club from being saved:</h2>

      <ul>
      <% @activity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <%= f.input :club_id, collection: Club.all, label: 'Topluluk Seçiniz.' %>
    </div>
  <div class="field">

    <%= f.input :title %>
  </div>
  <div class="field">

    <%= f.input :image %>
  </div>
  <div class="field">
 
    <%= f.input :description %>
  </div>
    <div class="field">
  <%= f.input :avatar, as: :file %>
  
  </div>
  <div class="field">

    <%= f.input :start_date %>
  </div>
  <div class="field">
    <%= f.input :finish_date %>
  </div>
  <div class="field">

    <%= f.input :address %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
